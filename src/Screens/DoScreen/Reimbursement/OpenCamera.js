import React from "react";
import {View, Text} from 'react-native'

export default function OpenCamera(){
    return(
        <View style={{flex:1}}>
            <Text>Camera</Text>
        </View>
    )
}